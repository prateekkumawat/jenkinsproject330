pipeline {
    agent {
        label "devops"
    }
    tools {
        maven 'mvn'
    }
    environment {
        NEXUS_URL = "http://18.61.237.59:8081"
    }

    stages{
        stage("Clean Workspace") { 
            steps {
                cleanWs()
            }
        }
        stage("Code Checkout"){
          steps{ 
            git url:"https://github.com/prateekkumawat/jenkinsproject330.git", branch: "artifacts"
          }
        }
        
        stage("Maven package build"){
            steps {
                sh 'mvn clean package'
                sh 'mvn install -f pom.xml'
                sh 'mvn -s ./settings.xml package install'
            }
        }
     
       stage("Artifacts Upload"){
            steps{
                script { 
                    nexusArtifactUploader(
                        protocol: 'http',
                        nexusVersion: 'nexus3',
                        groupId: 'mavencommends'
                        nexusUrl: NEXUS_URL,
                        credentialsId: 'nexuscreds',
                        version: '0.2.2-SNAPSHOT',
                        repository: 'maven-releases'
                        artifacts: [
                            [artifactId: mavencommends,
                            classifier: '',
                            file: 'mavencommands' + version + '.jar',
                            type: 'jar']
                        ]    
                    );
                }        
            }
        }

        
    }
}